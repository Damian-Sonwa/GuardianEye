{
  "buildCommand": "npm install --prefix ../.. && NODE_ENV=development npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --prefix ../.. && NODE_ENV=development npm install",
  "framework": "nextjs"
}

